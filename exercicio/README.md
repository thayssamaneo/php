# Análise dos comandos SQL no Postgres

## Onde os dados ficam armazenados?

Os dados ficam armazenados em um arquivo de extensão sql que pode tanto ser embutido na aplicação, quanto externamente (no dispositivo ou remoto). No caso do banco do postgres ele fica armazenado 

## Qual a diferença entre INSERT E SELECT?

## Como funciona a estrutura da tabela